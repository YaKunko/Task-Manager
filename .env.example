# PostgreSQL (used by the db service and interpolated into DATABASE_URL)
POSTGRES_USER=taskmanager
POSTGRES_PASSWORD=taskmanager
POSTGRES_DB=taskmanager

# Backend connection string (local dev outside Docker uses localhost)
# DATABASE_URL=postgresql+asyncpg://taskmanager:taskmanager@localhost:5432/taskmanager
